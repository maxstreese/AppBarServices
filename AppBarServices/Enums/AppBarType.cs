// The enum 'AppBarMode' is used to define whether the AppBar should operate in a standard fashion (always be visible)
// or in an auto-hide fashion (only be visible if it is selected).

namespace AppBarServices.Enums
{
    public enum AppBarType
    {
        Standard,
        AutoHide
    }
}
